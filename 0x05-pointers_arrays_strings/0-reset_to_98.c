#include <stdio.h>

/**
 * main - start point 
 * reset_to_98 - updates the value of the variable
 * @ip : pointer points to to 98
 * return : always 0
 */

void reset_to_98(int *ip);

/* int main(void)
{
int num = 402;
printf("n=%d\n", num);
reset_to_98(&num);
printf("n=%d\n", num);
return (0);
}
void reset_to_98(int *ip)
{
*ip = 98;
} */
